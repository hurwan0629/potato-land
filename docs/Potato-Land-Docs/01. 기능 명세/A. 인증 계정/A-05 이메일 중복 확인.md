# A-05 닉네임·이메일 가입 검증

## 목적

회원가입 최종 요청에서 닉네임과 선택 입력인 이메일의 형식·중복을 검증한다.

## 정책

- 이메일은 선택값이다.
- 이메일을 입력한 경우에만 형식·중복을 확인한다.
- 닉네임·이메일 전용 사전 확인 API는 두지 않는다. `POST /api/auth/signup` 서비스와 DB UNIQUE 제약이 최종 판정한다.

## 체크리스트

- [x] 이메일 형식 검증
- [x] 이메일 미입력 허용
- [x] signup 서비스 중복 검증
- [x] DB UNIQUE 최종 방어
