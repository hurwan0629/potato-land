# branch `Commit 6f40e85`

1. `auth/auth.router.js`
http메서드: `post`
미들웨어: 없음

2. `auth/auth.controller.js signup(req, res)`
메서드명: `signup`
특이사항: 내부적으로 asyncHandler을 통해서 async를 등록하고있음. success: true를 사용함.

`service.signupUser(req.body)`를 호출함

3. `auth/auth.service.js signupUser`
인자: body

- `validateSignup(body)`를 이용해서 올바른 데이터를 받아내려함.
	- `const requiredFields = ["name", "nickname", "loginId", "password", "passwordConfirm", "phone", "phoneVerificationId"];`
- `getPhoneVerified(data.phone, "SIGNUP")`를 이용해서 verified 상태를 확인하려함. 여기에서 실패했다면 에러를 반환함
- `findSignupConflict`를 이용해서 아이디, 닉네임, 전화번호, 이메일 등에서 충돌이 나는지 db를 통해서 중복 검사를 함. 여기에서 충돌이 있으면 409 에러 `AppError` 어플리케이션 에러 객체를 반환해줌
- 문제 없으면 바로 bcrypt를 씌워서 `passwordHash`를 만들어주게됨
- `body.data`로 받은 데이터와 `passwordHash`를 이용하여 createUser을 실행하게됨. 
- 이후에 문제가 없으면 redis를 이용해서 `deletePhoneVerified(전화번호, "SIGNUP")`를 이용하여 전화번호를 삭제하게 됨.
- 이후에 생성된 계정을 잘 보냄
> 회원가입쪽은 큰 문제는 없어보임

> 향후에 비로그인 회원만 접근 가능하게 `guestOnly`와 같은 미들웨어를 등록할 필요가 있음.

> DB로 사용자 가입 성공 후 Redis 등록이 실패하게되면 실패 응답이 가게됩니다. 